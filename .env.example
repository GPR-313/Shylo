# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Financial Modeling Prep — market data (https://financialmodelingprep.com)
FMP_API_KEY=

# Reserved for Phase 1 ingestion (news, social velocity). Add keys here as
# sensors come online, and document each one when you add it.
