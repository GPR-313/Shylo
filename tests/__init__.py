"""ARGUS test suite.

Stdlib `unittest`, no third-party dependency: the ledger stack is the spine of
this project and its tests must keep running when `requirements.txt` rots.

    python3 -m unittest discover -s tests -v
"""
