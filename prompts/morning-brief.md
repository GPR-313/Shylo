Run the Morning Brief.

Follow `skills/morning-brief/SKILL.md` exactly: fetch fresh data (no
recall-only numbers), check open and overdue ledger entries, check
`docs/error-patterns.md`, log every new prediction through
`scripts/ledger.py add` BEFORE writing, then write `data/briefs/<today>.md`
and commit the brief together with the ledger change. Apply all CLAUDE.md
pre-publication gates; if the impress test fails, dig deeper before shipping.
