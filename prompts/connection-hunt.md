Run a Connection Hunt.

Follow `skills/connection-hunt/SKILL.md` exactly: mine `python -m argus.graph
report` first (bridges, cut points, concentration, and especially memory ideas
never converted), then pick two domains you have not paired this month — the
pair you have been avoiding, not the one that looks interesting — and trace
chains from live, dated, primary-source signals on both sides. Every chain must
name its leading indicator L or it dies. Apply all four kill filters. Convert
survivors through the thesis-forge workflow and record why each casualty died.

A hunt that finds nothing and says so is a successful hunt. Manufacturing a
chain to avoid an empty output is worse than an empty output.
