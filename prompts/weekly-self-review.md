Run the Weekly Self-Review.

Follow `skills/weekly-self-review/SKILL.md` exactly: resolve every overdue
prediction against its own resolution criteria (fetch the named sources),
run `scripts/ledger.py score` and `scripts/calibration.py`, autopsy every
miss, update `docs/error-patterns.md`, propose at least one concrete process
upgrade, then write `data/reviews/<year>-W<week>.md` and commit the memo,
resolutions, and error-pattern updates together.
