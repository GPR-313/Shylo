Forge a thesis from the observation below.

Follow `skills/thesis-forge/SKILL.md` exactly, in order: state the mechanism as
a causal chain and apply the reversal and next-link tests; stage it honestly
against an attention series; check the prediction markets BEFORE logging and
record `--market-prob`; anchor every resolve-by date to a dated catalyst and
verify with `python -m argus.catalysts orphans`; register the thesis with its
kill criteria and memory links; run `python -m argus.graph concentration`
BEFORE sizing; then size with `python -m argus.edge`. Never invent --win/--loss.
Apply all CLAUDE.md pre-publication gates. If the observation fails step 0,
capture it to memory and say so rather than forcing a thesis.

Observation:
